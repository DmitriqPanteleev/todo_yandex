import 'package:flutter/material.dart';

abstract class C {
  // Colors for light theme
  // Back [Light]
  static const backPrimaryLight = Color.fromRGBO(247, 246, 242, 1);
  static const backSecondaryLight = Color.fromRGBO(255, 255, 255, 1);
  static const backElevatedLight = Color.fromRGBO(255, 255, 255, 1);

  // Just colors [Light]
  static const redLight = Color.fromRGBO(255, 59, 48, 1);
  static const greenLight = Color.fromRGBO(52, 199, 89, 1);
  static const blueLight = Color.fromRGBO(0, 122, 255, 1);
  static const grayLight = Color.fromRGBO(142, 142, 147, 1);
  static const lightGrayLight = Color.fromRGBO(209, 209, 214, 1);
  static const whiteLight = Color.fromRGBO(255, 255, 255, 1);

  // Label [Light]
  static const labelPrimaryLight = Color.fromRGBO(0, 0, 0, 1);
  static const labelSecondaryLight = Color.fromRGBO(0, 0, 0, 0.6);
  static const labelTertiaryLight = Color.fromRGBO(0, 0, 0, 0.3);
  static const labelDisableLight = Color.fromRGBO(0, 0, 0, 0.15);

  // Support [Light]
  static const separatorLight = Color.fromRGBO(0, 0, 0, 0.2);
  static const overlayLight = Color.fromRGBO(0, 0, 0, 0.06);

  // Colors for dark theme
  // Back [Dark]
  static const backPrimaryDark = Color.fromRGBO(22, 22, 24, 1);
  static const backSecondaryDark = Color.fromRGBO(37, 37, 40, 1);
  static const backElevatedDark = Color.fromRGBO(60, 60, 63, 1);

  // Just colors [Dark]
  static const redDark = Color.fromRGBO(255, 69, 58, 1);
  static const greenDark = Color.fromRGBO(50, 215, 75, 1);
  static const blueDark = Color.fromRGBO(10, 132, 255, 1);
  static const grayDark = Color.fromRGBO(142, 142, 147, 1);
  static const lightGrayDark = Color.fromRGBO(72, 72, 74, 1);
  static const whiteDark = Color.fromRGBO(255, 255, 255, 1);

  // Label [Dark]
  static const labelPrimaryDark = Color.fromRGBO(255, 255, 255, 1);
  static const labelSecondaryDark = Color.fromRGBO(255, 255, 255, 0.6);
  static const labelTertiaryDark = Color.fromRGBO(255, 255, 255, 0.4);
  static const labelDisableDark = Color.fromRGBO(255, 255, 255, 0.15);

  // Support [Dark]
  static const separatorDark = Color.fromRGBO(0, 0, 0, 0.2);
  static const overlayDark = Color.fromRGBO(0, 0, 0, 0.32);
}
